# #################################################################################################
# FlatCAM Evo Beta — Python Dependencies
# Verified to work with Python 3.12.x (recommended)
# Also works with Python 3.11.x
# Python 3.14 may lack binary wheels for VisPy / PyOpenGL / GDAL
# #################################################################################################
#
# Install:  pip install -r requirements.txt
#
# GDAL and rasterio CANNOT be installed via pip on Windows without GDAL C headers.
# They are only needed for the Image Import plugin (raster → PCB trace).
# Windows users: download pre-built wheels from https://github.com/cgohlke/geospatial-wheels
#   then: pip install GDAL-<ver>-cp312-...-win_amd64.whl rasterio-<ver>-...-win_amd64.whl
#
# pyqtdarktheme is NOT listed here because the project bundles a pinned copy in libs/qdarktheme/.
# Do not pip-install pyqtdarktheme — it will conflict with the bundled version.
# #################################################################################################


# --- Core numerics and geometry ---
numpy>=1.16
shapely>=2.0
rtree


# --- Qt GUI ---
pyqt6>=6.4.0
pyopengl
# VisPy: upper-bound set because appGUI/VisPyPatches.py uses private VisPy internals
# that may change in 0.17+. Raise the cap after testing each new minor release.
vispy>=0.9.0,<0.17
freetype-py


# --- Plotting / rendering ---
matplotlib>=3.5.0
cycler>=0.10
kiwisolver>=1.1
python-dateutil>=2.1


# --- File format support ---
lxml
svg.path>=4.0
svglib
fontTools
ezdxf
pikepdf>=2.0
reportlab>=3.5


# --- Image import (optional — requires GDAL; see header for Windows instructions) ---
# gdal
# rasterio
# svgtrace   # depends on pyppeteer which is abandoned; may not work at runtime


# --- Path optimisation (optional but recommended) ---
# OR-Tools 7.x API is incompatible with 9.x; require 9+ to match installed reality
ortools>=9.0


# --- App utilities ---
simplejson
dill
qrcode>=6.1
pyserial>=3.4
darkdetect


# --- Headless HTTP service ---
fastapi>=0.115
uvicorn>=0.30


# --- Dark mode detection ---
# (dark/light theme is applied via the bundled libs/qdarktheme — no separate install needed)
